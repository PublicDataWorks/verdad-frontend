import { createRoutesFromElements, Route } from 'react-router-dom';


const AppRoutes = createRoutesFromElements(
  <Route>
    <Route path="/" element={<b>asdsd</b>} />
  </Route>,
);

export default AppRoutes;
